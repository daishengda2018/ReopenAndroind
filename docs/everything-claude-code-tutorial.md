# Everything Claude Code 使用教程

## 📚 目录
1. [核心概念](#核心概念)
2. [可用的 Agents](#可用的-agents)
3. [开发任务流程](#开发任务流程)
4. [实战示例](#实战示例)
5. [最佳实践](#最佳实践)

---

## 核心概念

### 1. Skills（技能）
- **位置**: `~/.claude/skills/` 和项目中的 `.claude/skills/`
- **作用**: 定义工作流程、编码规范、最佳实践
- **触发**: 自动应用或通过斜杠命令触发

**您的项目已有技能**:
- `.claude/skills/SKILL.md` - ReopenAndroid 项目专属技能
- 包含 UDF 架构、Room 数据库、Hilt DI 等模式

### 2. Commands（命令）
- **位置**: `~/.claude/commands/`
- **作用**: 快速执行的特定任务
- **触发**: 通过 `/命令名` 调用

**常用命令**:
```bash
/plan          - 创建实现计划
/tdd           - 测试驱动开发
/refactor      - 代码重构
/code-review   - 代码审查
/security      - 安全审查
/e2e           - 端到端测试
```

### 3. Hooks（钩子）
- **作用**: 在特定事件自动触发
- **类型**:
  - `PreToolUse` - 工具执行前
  - `PostToolUse` - 工具执行后
  - `Stop` - 会话结束时

**示例**: 自动格式化代码、运行测试、显示警告

### 4. Instincts（本能）
- **位置**: `~/.claude/homunculus/instincts/`
- **作用**: 基于项目历史自动应用的编码模式
- **置信度**: 0-100%，表示应用强度

**您的项目已有 6 个激活的本能**:
- UDF 架构 (95%)
- 不可变状态 (95%)
- Room 数据库 (90%)
- Hilt DI (90%)
- 组件组织 (85%)
- Git 提交约定 (75%)

### 5. Agents（代理）
- **位置**: `~/.claude/agents/`
- **作用**: 专门处理特定任务的子代理
- **优势**: 节省主上下文、并行执行、专业化

---

## 可用的 Agents

### 🏗️ 架构与规划

#### `/plan` - Planner Agent
**用途**: 创建功能实现计划

```bash
/plan 添加一个新的"预测统计"功能
```

**触发时机**:
- 复杂功能（多个文件、多种技术）
- 不确定实现方式
- 需要架构决策

**输出**:
- 分步实施计划
- 依赖关系分析
- 风险评估

#### `architect` - Architect Agent
**用途**: 系统设计和架构决策

```bash
# 自动触发，当涉及架构改动时
```

**适用场景**:
- 数据库设计
- API 设计
- 模块划分

### 🔧 开发与测试

#### `/tdd` - TDD Guide Agent
**用途**: 测试驱动开发工作流

```bash
/tdd 实现用户登录功能
```

**工作流程**:
1. 先写测试（RED）
2. 实现功能（GREEN）
3. 重构优化（IMPROVE）
4. 验证覆盖率 ≥ 80%

#### `e2e-runner` - E2E Testing Agent
**用途**: 端到端测试

```bash
/e2e 测试完整的用户注册流程
```

**功能**:
- 使用 Playwright
- 自动发现测试用例
- 生成测试报告

### 📊 代码质量

#### `/code-review` - Code Reviewer Agent
**用途**: 代码质量审查

```bash
/code-review
```

**检查项**:
- 代码可读性
- 安全性
- 性能
- 可维护性

#### `/security` - Security Reviewer Agent
**用途**: 安全漏洞检测

```bash
/security
```

**检查范围**:
- OWASP Top 10
- SQL 注入
- XSS 攻击
- 硬编码密钥

### 🐛 调试与修复

#### `build-error-resolver` - Build Error Resolver
**用途**: 修复构建错误

```bash
# 自动触发，当构建失败时
```

**支持**:
- TypeScript 错误
- 编译错误
- 依赖问题

#### `refactor-cleaner` - Refactor Cleaner
**用途**: 清理死代码

```bash
/refactor-clean
```

**功能**:
- 检测未使用的代码
- 安全删除
- 运行分析工具

### 📝 文档

#### `doc-updater` - Documentation Updater
**用途**: 更新文档和代码地图

```bash
/doc-update
```

**更新内容**:
- CODEMAPS
- README
- API 文档

---

## 开发任务流程

### 🎯 标准开发流程

```
1. 需求分析
   ↓
2. 创建计划 (/plan)
   ↓
3. TDD 开发 (/tdd)
   ↓
4. 代码审查 (/code-review)
   ↓
5. 安全审查 (/security)
   ↓
6. 提交代码
```

### 🚀 快速开发流程（小功能）

```
1. 直接编码
   ↓
2. 本能自动指导
   ↓
3. 提交代码
```

---

## 实战示例

### 示例 1: 添加新功能（复杂）

**任务**: 添加"趋势预测分析"功能

#### 步骤 1: 创建计划

```bash
/plan 添加趋势预测分析功能
```

**Planner Agent 会**:
- 分析现有代码结构
- 设计 UDF 状态管理
- 规划组件层次
- 识别依赖关系
- 创建 TODO 列表

#### 步骤 2: 使用 TDD 开发

```bash
/tdd 实现趋势预测 ViewModel 和 UI
```

**TDD Guide Agent 会**:
1. **先写测试**:
   ```kotlin
   @Test
   fun testTrendPrediction() {
       // Given
       val history = listOf(/* 测试数据 */)

       // When
       val prediction = analyzer.predict(history)

       // Then
       assertNotNull(prediction)
       assertEquals(expected, prediction.trend)
   }
   ```

2. **实现功能**:
   - 在 `GameUiState.kt` 添加状态
   - 在 `GameUiEvent.kt` 定义事件
   - 在 `GameViewModel.kt` 实现逻辑
   - 创建 `TrendPredictionDisplay.kt` 组件

3. **验证覆盖率**:
   ```bash
   ./gradlew test
   ./gradlew jacocoTestReport
   ```

#### 步骤 3: 代码审查

```bash
/code-review
```

**Code Reviewer Agent 会检查**:
- ✅ 遵循 UDF 架构
- ✅ 组件按功能组织
- ✅ 状态更新不可变
- ✅ 代码可读性
- ✅ 性能优化建议

#### 步骤 4: 安全审查

```bash
/security
```

**Security Reviewer Agent 会检查**:
- ✅ 无 SQL 注入风险
- ✅ 无硬编码密钥
- ✅ 输入验证
- ✅ 数据加密

#### 步骤 5: 提交

根据本能自动生成提交信息:
```bash
feat: add trend prediction analysis

- Implement prediction algorithm in ViewModel
- Add TrendPredictionDisplay component
- Write unit tests with 85% coverage
- Follow UDF architecture pattern

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

---

### 示例 2: 修复 Bug（简单）

**任务**: 修复计数器不更新的问题

#### 步骤 1: 直接编码

本能会自动指导：

```kotlin
// ❌ 错误写法（本能会警告）
_uiState.value.counter.value = newValue

// ✅ 正确写法（本能建议）
_uiState.update { it.copy(
    counter = it.counter.copy(value = newValue)
)}
```

#### 步骤 2: 提交

```bash
fix: counter not updating properly

Use immutable state update with copy() method
instead of direct mutation.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

---

### 示例 3: 数据库迁移

**任务**: 在 GameSessionEntity 添加 `playerName` 字段

本能会自动应用 **Room Database Workflow**:

#### 1️⃣ 修改实体
```kotlin
@Entity(tableName = "game_sessions")
data class GameSessionEntity(
    @PrimaryKey(autoGenerate = true)
    val id: Long = 0,
    val timestamp: Long,
    val playerName: String  // 新增
)
```

#### 2️⃣ 更新 DAO（如需要）
```kotlin
@Dao
interface GameSessionDao {
    @Query("SELECT * FROM game_sessions WHERE playerName = :name")
    fun getByPlayer(name: String): Flow<List<GameSessionEntity>>
}
```

#### 3️⃣ 增加版本号
```kotlin
@Database(
    entities = [GameSessionEntity::class, /* ... */],
    version = 2,  // 1 → 2
    exportSchema = true
)
abstract class AppDatabase : RoomDatabase()
```

#### 4️⃣ 添加迁移
```kotlin
val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(database: SupportSQLiteDatabase) {
        database.execSQL(
            "ALTER TABLE game_sessions ADD COLUMN playerName TEXT NOT NULL DEFAULT ''"
        )
    }
}
```

#### 5️⃣ 构建生成 Schema
```bash
./gradlew assembleDebug
```

#### 6️⃣ 验证 Schema
```bash
ls app/schemas/com.dsd.baccarat.data.room.AppDatabase/
# 应该看到: 1.json  2.json
```

---

## 最佳实践

### ✅ DO（应该做的）

1. **复杂功能先用 `/plan`**
   - 多个文件改动
   - 架构调整
   - 不确定实现方案

2. **使用 `/tdd` 开发新功能**
   - 测试先行
   - 覆盖率 ≥ 80%
   - Red → Green → Improve

3. **编码后自动审查**
   ```bash
   /code-review  # 质量检查
   /security     # 安全检查
   ```

4. **相信本能的指导**
   - 本能基于 200 条提交学习
   - 置信度越高，越应该遵循
   - 遵循项目既有模式

5. **使用 TodoWrite 跟踪进度**
   - 创建任务列表
   - 标记完成状态
   - 保持进度可见

### ❌ DON'T（不应该做的）

1. **不要跳过计划**（复杂功能）
   - 直接编码可能导致返工
   - `/plan` 能识别潜在问题

2. **不要忽略本能建议**
   - 本能防止常见错误
   - 保持代码一致性

3. **不要忘记测试**
   - TDD 确保质量
   - 覆盖率很重要

4. **不要跳过代码审查**
   - 人会犯错
   - Agent 能发现问题

5. **不要硬编码**
   - 使用依赖注入
   - 遵循 Hilt 模式

---

## 快速参考

### 常用命令速查

| 命令 | 用途 | 何时使用 |
|------|------|----------|
| `/plan` | 创建实现计划 | 复杂功能 |
| `/tdd` | 测试驱动开发 | 新功能 |
| `/code-review` | 代码审查 | 编码完成后 |
| `/security` | 安全审查 | 涉及敏感数据 |
| `/refactor-clean` | 清理死代码 | 代码维护 |
| `/e2e` | 端到端测试 | 关键流程 |
| `/instinct-status` | 查看本能状态 | 检查学习情况 |

### Agent 自动触发条件

| Agent | 自动触发条件 |
|-------|------------|
| `planner` | 复杂功能请求 |
| `build-error-resolver` | 构建失败时 |
| `code-reviewer` | 编写代码后（建议） |
| `security-reviewer` | 处理用户输入时（建议） |
| `architect` | 架构决策时 |

### 本能自动应用

| 场景 | 触发的本能 | 置信度 |
|------|-----------|--------|
| 添加 UI 功能 | UDF 架构 | 95% |
| 更新状态 | 不可变状态 | 95% |
| 创建组件 | 组件组织 | 85% |
| 修改数据库 | Room 工作流 | 90% |
| 添加依赖 | Hilt DI | 90% |
| 提交代码 | Git 约定 | 75% |

---

## 高级技巧

### 1. 并行执行多个 Agents

```markdown
# 同时启动代码审查和安全审查
Launch 2 agents in parallel:
1. Code reviewer - check quality
2. Security reviewer - check vulnerabilities
```

### 2. 后台运行长时间任务

```markdown
# 在后台运行测试，继续其他工作
Run tests in background:
```

### 3. 使用 Hooks 自动化

```json
{
  "PostToolUse": [
    {
      "matcher": "tool == \"Edit\" && tool_input.file_path ends with \".kt\"",
      "hooks": [
        {
          "type": "command",
          "command": "echo 'Kotlin file edited, consider running tests'"
        }
      ]
    }
  ]
}
```

---

## 总结

### 关键要点

1. **本能是基础** - 6 个本能自动指导日常编码
2. **计划是关键** - 复杂功能先用 `/plan`
3. **TDD 是保障** - `/tdd` 确保代码质量
4. **审查是必须** - `/code-review` 和 `/security`
5. **持续改进** - 本能会随着项目发展学习

### 您的装备

✅ **项目技能**: `.claude/skills/SKILL.md`
✅ **6 个激活本能**: 架构、状态、数据库、DI、组织、Git
✅ **专业 Agents**: 规划、开发、测试、审查、安全
✅ **自动化 Hooks**: 可自定义工作流程

### 下一步

1. **尝试小任务** - 让本能自动指导
2. **使用 `/plan`** - 规划一个中等复杂度功能
3. **实践 TDD** - 用 `/tdd` 开发新功能
4. **建立习惯** - 编码后自动审查

---

**祝您开发愉快！🚀**

有问题随时问我，我会根据您的项目本能和 Everything Claude Code 的最佳实践来帮助您。
